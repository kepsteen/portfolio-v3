export const metadata = {
	slug: "next-app-router-notes",
	title: "Notes on the App Router",
	date: "2025-03-01",
	excerpt:
		"Server Components by default, colocated data fetching, and keeping client boundaries small—patterns that have worked well on this portfolio.",
	tags: ["Next.js", "React"],
};

## Server first

Leaning on React Server Components keeps the first paint light. Interactive pieces stay behind small client components.

## Colocation

Route segments own their data and UI. When something moves, the folder structure makes the blast radius obvious.
